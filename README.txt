Clone the Repository: Clone the project repository to your local machine.

Docker Compose: Run the following command to build and start the services:

bash
Copy code
docker-compose up --build
Access the Services:

Generator service: Accessible at http://localhost:5001/generate
Invoker service: Accessible at http://localhost:5002/recommend